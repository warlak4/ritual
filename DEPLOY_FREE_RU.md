# Бесплатные способы деплоя проекта в сеть (для России)

## 🌟 Рекомендуемые варианты

### 1. **Yandex Cloud** (Рекомендуется для России)

**Плюсы:**
- ✅ Полностью доступен из России
- ✅ Бесплатный стартовый пакет: 4000₽ на 60 дней
- ✅ Поддержка Node.js, Docker
- ✅ Можно использовать Managed PostgreSQL вместо SQL Server
- ✅ Быстрая скорость для российских пользователей

**Как деплоить:**
1. Зарегистрируйтесь на [cloud.yandex.ru](https://cloud.yandex.ru)
2. Создайте сервисный аккаунт
3. Используйте **Yandex Cloud Functions** или **Compute Cloud** (VM)
4. Для БД: **Managed PostgreSQL** (бесплатно первые 60 дней)

**Конфигурация:**
- Backend: Docker контейнер на Compute Cloud или Cloud Functions
- Frontend: Static hosting в Object Storage + Cloud CDN
- БД: Managed PostgreSQL (можно мигрировать схему из SQL Server)

---

### 2. **Selectel** (Российский провайдер)

**Плюсы:**
- ✅ Российский провайдер, полностью доступен
- ✅ Бесплатный тестовый период VPS
- ✅ Поддержка Docker
- ✅ Хорошая документация на русском

**Как деплоить:**
1. Зарегистрируйтесь на [selectel.ru](https://selectel.ru)
2. Создайте VPS (есть бесплатный тестовый период)
3. Установите Docker и запустите через `docker-compose`
4. Используйте их Object Storage для статики frontend

---

### 3. **Timeweb Cloud** (Российский провайдер)

**Плюсы:**
- ✅ Российский провайдер
- ✅ Бесплатный тестовый период
- ✅ VPS с поддержкой Node.js
- ✅ Простая панель управления

**Как деплоить:**
1. Зарегистрируйтесь на [timeweb.cloud](https://timeweb.cloud)
2. Создайте VPS (бесплатный тест)
3. Установите Node.js и запустите backend
4. Используйте их хостинг для статики

---

### 4. **GitHub Pages** (Только Frontend)

**Плюсы:**
- ✅ Полностью бесплатно
- ✅ Работает из России (через VPN при необходимости)
- ✅ Автоматический деплой из GitHub
- ✅ Бесплатный SSL

**Ограничения:**
- ❌ Только статические сайты (frontend)
- ❌ Backend нужно размещать отдельно

**Как использовать:**
1. Соберите frontend: `cd frontend && npm run build`
2. В настройках репозитория GitHub: Settings → Pages
3. Выберите branch `main` и папку `frontend/dist`
4. Frontend будет доступен по адресу: `https://warlak4.github.io/ritual/`

---

### 5. **Vercel** (Может работать через VPN)

**Плюсы:**
- ✅ Отличная поддержка Next.js/React
- ✅ Бесплатный план с хорошими лимитами
- ✅ Автоматический деплой из GitHub
- ✅ Edge Functions для API

**Ограничения:**
- ⚠️ Может быть заблокирован, нужен VPN для доступа
- ⚠️ Backend лучше выносить отдельно

**Как использовать:**
1. Зарегистрируйтесь на [vercel.com](https://vercel.com) (через VPN)
2. Подключите GitHub репозиторий
3. Frontend деплоится автоматически
4. Backend можно деплоить через Vercel Serverless Functions

---

### 6. **Netlify** (Может работать через VPN)

**Плюсы:**
- ✅ Отличная поддержка статических сайтов
- ✅ Бесплатный план
- ✅ Netlify Functions для backend логики
- ✅ Автоматический деплой

**Ограничения:**
- ⚠️ Может быть заблокирован, нужен VPN
- ⚠️ Ограничения на serverless functions

---

### 7. **Railway.app** (Может работать через VPN)

**Плюсы:**
- ✅ Отличная поддержка Docker
- ✅ Бесплатный план ($5 кредитов в месяц)
- ✅ Автоматический деплой из GitHub
- ✅ Поддержка PostgreSQL

**Ограничения:**
- ⚠️ Может быть заблокирован
- ⚠️ Ограниченные бесплатные кредиты

---

### 8. **Fly.io** (Может работать через VPN)

**Плюсы:**
- ✅ Бесплатный план (3 shared VMs)
- ✅ Отличная поддержка Docker
- ✅ Глобальная сеть

**Ограничения:**
- ⚠️ Может быть заблокирован
- ⚠️ Ограниченные ресурсы на бесплатном плане

---

## 🎯 Рекомендуемый план действий для России

### Вариант 1: Полностью российский стек (Рекомендуется)

1. **Backend**: Yandex Cloud Compute Cloud (VM) или Cloud Functions
2. **Frontend**: Yandex Object Storage + Cloud CDN
3. **БД**: Yandex Managed PostgreSQL (миграция схемы из SQL Server)

**Преимущества:**
- ✅ Полностью доступен из России
- ✅ Быстрая скорость
- ✅ Бесплатный стартовый пакет
- ✅ Русскоязычная поддержка

### Вариант 2: Гибридный подход

1. **Backend**: Selectel или Timeweb VPS (бесплатный тест)
2. **Frontend**: GitHub Pages (работает через VPN при необходимости)
3. **БД**: PostgreSQL на том же VPS или отдельный Managed PostgreSQL

### Вариант 3: Только российские провайдеры

1. **Backend + Frontend**: Selectel VPS с Docker
2. **БД**: PostgreSQL в Docker контейнере
3. Используйте Nginx для раздачи статики frontend

---

## 📋 Инструкции по миграции с SQL Server на PostgreSQL

Если выбран вариант с PostgreSQL, нужно:

1. **Установить PostgreSQL драйвер в backend:**
```bash
cd backend
npm install pg
npm uninstall mssql
```

2. **Обновить database.config.ts:**
```typescript
// Заменить TypeORM конфигурацию с mssql на postgres
type: 'postgres',
```

3. **Адаптировать SQL скрипты:**
- SQL Server синтаксис → PostgreSQL синтаксис
- Использовать инструменты миграции (TypeORM migrations)

---

## 🔧 Быстрый старт с Yandex Cloud

### 1. Создание VM для Backend

```bash
# Установите Yandex CLI
curl -sSL https://storage.yandexcloud.net/yandexcloud-yc/install.sh | bash

# Инициализация
yc init

# Создание VM
yc compute instance create \
  --name ritual-backend \
  --zone ru-central1-a \
  --network-interface subnet-name=default-ru-central1-a,nat-ip-version=ipv4 \
  --create-boot-disk image-folder-id=standard-images,image-family=ubuntu-2204,size=20 \
  --ssh-key ~/.ssh/id_rsa.pub
```

### 2. Деплой через Docker

```bash
# На VM
git clone https://github.com/warlak4/ritual.git
cd ritual
docker-compose -f ops/docker-compose.yml up -d
```

### 3. Frontend в Object Storage

```bash
# Сборка frontend
cd frontend
npm run build

# Загрузка в Object Storage через консоль Yandex Cloud
# Или используйте yc CLI для загрузки
```

---

## 💡 Альтернатива: Собственный VPS

Если есть возможность, можно использовать:

1. **Hetzner** (дешевые VPS, ~4€/месяц)
2. **DigitalOcean** (есть промокоды на бесплатные кредиты)
3. **Contabo** (дешевые VPS)

Но для России лучше использовать российские провайдеры для лучшей скорости.

---

## ⚠️ Важные замечания

1. **Бесплатные планы имеют ограничения:**
   - Ограниченные ресурсы (CPU, RAM, трафик)
   - Ограничения по времени работы
   - Могут "засыпать" после неактивности

2. **Для продакшн рекомендуется:**
   - Платный план (от 300-500₽/месяц)
   - Резервное копирование
   - Мониторинг

3. **Безопасность:**
   - Настройте firewall
   - Используйте HTTPS
   - Регулярно обновляйте зависимости

---

## 📞 Поддержка

Если возникнут вопросы по деплою на конкретную платформу, обращайтесь к документации провайдера или их техподдержке.

