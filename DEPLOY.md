# Инструкция по деплою проекта на Render.com

## Шаг 1: Подготовка репозитория

Проект уже настроен и загружен в GitHub: https://github.com/warlak4/ritual.git

## Шаг 2: Регистрация на Render.com

1. Перейдите на [render.com](https://render.com)
2. Зарегистрируйтесь или войдите через GitHub
3. Подключите ваш GitHub аккаунт к Render

## Шаг 3: Создание сервисов через Blueprint

1. В Dashboard Render нажмите **"New"** → **"Blueprint"**
2. Выберите репозиторий `warlak4/ritual`
3. Render автоматически обнаружит файл `render.yaml` и создаст два сервиса:
   - `ritual-backend` (Backend API)
   - `ritual-frontend` (Frontend SPA)

## Шаг 4: Настройка базы данных

⚠️ **Важно**: SQL Server требует отдельного хостинга. Варианты:

### Вариант A: Azure SQL Database (рекомендуется)
1. Создайте Azure SQL Database на [portal.azure.com](https://portal.azure.com)
2. Получите строку подключения:
   - Host: `your-server.database.windows.net`
   - Port: `1433`
   - Database: `RitualDB`
   - Username: ваш логин
   - Password: ваш пароль

### Вариант B: Внешний SQL Server
- Используйте существующий SQL Server (локальный или VPS)
- Убедитесь, что он доступен из интернета
- Настройте firewall для разрешения подключений

### Вариант C: Docker SQL Server (для тестирования)
- Можно использовать отдельный VPS с Docker
- Запустите SQL Server контейнер с публичным доступом

## Шаг 5: Настройка переменных окружения

### Backend сервис (`ritual-backend`)

В Dashboard Render откройте сервис `ritual-backend` → **Environment** и добавьте:

```
DB_HOST=your-sql-server-host.database.windows.net
DB_PORT=1433
DB_USER=your-db-username
DB_PASSWORD=your-secure-password
DB_NAME=RitualDB
DB_ENCRYPT=true
DB_TRUST_CERT=true
DB_LOGGING=false
CORS_ORIGINS=https://ritual-frontend.onrender.com
JWT_SECRET=<сгенерируйте случайную строку минимум 32 символа>
CRYPTO_MASTER_KEY=<32-байтная hex строка, например: 0123456789abcdef0123456789abcdef>
TELEGRAM_BOT_TOKEN=<опционально, если используете Telegram бота>
TELEGRAM_CHAT_ID=<опционально>
```

**Важно**: 
- `JWT_SECRET` и `CRYPTO_MASTER_KEY` должны быть уникальными и секретными
- `CORS_ORIGINS` должен содержать URL вашего frontend (будет доступен после деплоя)

### Frontend сервис (`ritual-frontend`)

После того как backend будет задеплоен, получите его URL (например: `https://ritual-backend.onrender.com`) и добавьте:

```
VITE_API_BASE_URL=https://ritual-backend.onrender.com
```

## Шаг 6: Инициализация базы данных

После настройки подключения к БД, выполните SQL скрипты из `db/scripts/`:

1. Подключитесь к вашей SQL Server базе данных
2. Выполните скрипты по порядку:
   ```sql
   -- 00_prerequisites.sql
   -- 01_tables.sql
   -- 02_views.sql
   -- 03_security.sql
   -- 04_programmability.sql
   -- 05_triggers.sql
   -- 06_seed.sql (опционально, для тестовых данных)
   ```

Можно использовать:
- Azure Data Studio
- SQL Server Management Studio (SSMS)
- `sqlcmd` утилиту
- Или любой другой SQL клиент

## Шаг 7: Деплой

1. После настройки всех переменных окружения, Render автоматически начнет деплой
2. Или нажмите **"Manual Deploy"** → **"Deploy latest commit"**
3. Дождитесь завершения сборки (обычно 5-10 минут)

## Шаг 8: Проверка работы

После успешного деплоя:

- **Backend API**: `https://ritual-backend.onrender.com`
  - Swagger документация: `https://ritual-backend.onrender.com/api/docs`
  - Health check: `https://ritual-backend.onrender.com/health` (если настроен)

- **Frontend**: `https://ritual-frontend.onrender.com`
  - Откройте в браузере и проверьте работу приложения

## Шаг 9: Обновление CORS (если нужно)

Если frontend работает на другом домене, обновите `CORS_ORIGINS` в backend:

```
CORS_ORIGINS=https://ritual-frontend.onrender.com,https://your-custom-domain.com
```

## Важные замечания

### Бесплатный план Render
- ⚠️ Сервисы на бесплатном плане "засыпают" после 15 минут неактивности
- Первый запрос после простоя может занять 30-60 секунд (cold start)
- Для продакшн используйте платный план ($7/месяц за сервис)

### Безопасность
- 🔒 Никогда не коммитьте `.env` файлы в git
- 🔒 Используйте сильные пароли для БД
- 🔒 Генерируйте уникальные `JWT_SECRET` и `CRYPTO_MASTER_KEY`
- 🔒 Настройте firewall для SQL Server (разрешите только IP Render)

### Мониторинг
- Render предоставляет логи в реальном времени
- Настройте алерты для критических ошибок
- Мониторьте использование ресурсов

## Troubleshooting

### Backend не запускается
- Проверьте логи в Render Dashboard
- Убедитесь, что все переменные окружения установлены
- Проверьте подключение к БД (host, port, credentials)

### Frontend не подключается к Backend
- Проверьте `VITE_API_BASE_URL` в frontend
- Убедитесь, что backend доступен по указанному URL
- Проверьте CORS настройки в backend

### Ошибки подключения к БД
- Проверьте доступность SQL Server из интернета
- Убедитесь, что firewall разрешает подключения с IP Render
- Проверьте правильность credentials

## Дополнительные ресурсы

- [Render Documentation](https://render.com/docs)
- [Azure SQL Database](https://docs.microsoft.com/azure/azure-sql/)
- [NestJS Deployment](https://docs.nestjs.com/recipes/deployment)

